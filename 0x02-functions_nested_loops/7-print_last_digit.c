#include "main.h"

/**
 * print_last_digit - function that prints last digit
 * of a number
 * @n: The value of number to check
 * Return: value of the last digit
 */
int print_last_digit(int n)
{
	if (n != NULL)
	{
		return (n % 10);
	}
}
